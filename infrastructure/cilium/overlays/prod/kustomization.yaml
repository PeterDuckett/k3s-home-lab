apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - bgp-config.yaml

patches:
  - path: values-patch.yaml
    target:
      kind: HelmRelease
      name: cilium