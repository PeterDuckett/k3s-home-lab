apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - k3s-server-plan.yaml
  - k3s-agent-plan.yaml

# Crucial overlay rule: Forces the local plans to run where the official chart lives
namespace: cattle-system