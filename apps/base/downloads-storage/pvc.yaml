apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-ssd
  namespace: system
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-ssd
  resources:
    requests:
      storage: 800Gi
