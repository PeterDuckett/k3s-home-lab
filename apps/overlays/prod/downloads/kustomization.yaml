apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/downloads-storage
  - qbittorrent
  - nzbget
  - autobrr
  - bazarr
  - lidarr
  - prowlarr
  - radarr
  - readarr
  - wizarr
  # postarr intentionally left out -- no official/trustworthy image
  # source found, needs clarification before deploying.
  #
  # The remaining app dirs in this folder (doplarr, homarr, jackett,
  # maintainerr, mylar, notifiarr, organizrr, overseerr, recyclarr,
  # sonarr, tdarr) still contain the placeholder kustomization.yaml
  # copied from the traefik overlay and are not wired up yet.
