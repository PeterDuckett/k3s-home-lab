apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/downloads-storage
  - qbittorrent
  - nzbget
  # The remaining app dirs in this folder (autobrr, bazarr, doplarr, homarr,
  # jackett, lidarr, maintainerr, mylar, notifiarr, organizrr, overseerr,
  # postarr, prowlarr, radarr, readarr, recyclarr, sonarr, tdarr, wizarr)
  # still contain the placeholder kustomization.yaml copied from the traefik
  # overlay and are not wired up yet — out of scope for this change.
