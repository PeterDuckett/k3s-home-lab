apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/downloads-storage
  - qbittorrent
  - nzbget
  - autobrr
  - bazarr
  - lidarr
  - prowlarr
  - radarr
  - wizarr
  - sonarr
  - overseerr
  - notifiarr
  - recyclarr
  - doplarr
  - mylar
  - tdarr
  - maintainerr
  # postarr intentionally left out -- no official/trustworthy image
  # source found, needs clarification before deploying.
  #
  # readarr and plaxt removed -- no arm64 image available for either
  # (readarr:develop has no arm64 manifest, plaxt image is amd64-only),
  # permanently broken on this cluster's arm64 nodes.
  #
  # The remaining app dirs in this folder (homarr, jackett, organizrr)
  # still contain the placeholder kustomization.yaml copied from the
  # traefik overlay and are not wired up yet.
